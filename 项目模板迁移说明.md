# 项目模板迁移说明

本文件用于说明：如何从当前仓库抽取一套可复用的新项目模板，并让 `Taotaotao` 的 AI 或后续接手人只通过这一份文件，就能理解迁移目标、边界和稳定规则。

---

## 1. 文件目的

这是一份“从当前生图测试仓抽取新项目模板”的迁移说明，不是测试记录，也不是操作流水账。

本次迁移目标是：

- 采用完整模板迁移
- 采用半抽象落法
- 保留当前仓已验证有效的阶段化测试结构
- 不把当前项目的具体样例、具体测试产物一并搬走

这份文件的目标读者包括：

- 新项目的接手人
- 需要帮忙梳理迁移方案的 `Taotaotao` AI
- 后续需要复用这套测试工程骨架的人

---

## 2. 建议迁移的内容

建议迁移的内容：

- 目录骨架
- 治理骨架
- 阶段结构
- 方案子结构
- 索引与规则文档

明确不迁移的内容：

- 具体测试输出图
- 具体轮次问题结论
- 当前角色绑定样例
- 临时操作原文
- 仅对当前仓成立的个别测试上下文

新项目模板建议保留以下四段结构：

1. 公共输入
2. 提示词层
3. 阶段执行层
4. 治理层

按当前仓的稳定形态，建议迁移时优先保留这些目录：

- `00-公共资源/`
- `prompts/`
- `01-第一阶段-线稿/`
- `02-第二阶段-上色/`
- `03-第三阶段-动作/`
- `.agents/`
- `docs/`

其中：

- `00-公共资源/` 负责公共素材、人物资料、体裁定义、原始输入
- `prompts/` 负责阶段0到阶段3的提示词与节点定义
- `01/02/03` 阶段目录负责测试执行、输出结果与记录沉淀
- `.agents/` 负责规则、连续接手、过程记录
- `docs/` 负责结构说明、索引和目录视图

---

## 3. 必须保留的稳定规则

以下规则应视为模板级稳定规则，迁移时必须保留：

### 3.1 测试记录规则

- 每一轮测试只保留一个 `第xx轮-测试记录.md`
- 同一文件内按需分段记录：`基本信息`、`归档文件`、`问题总结`、`阶段结论`
- 不再拆分同一轮的 `问题总结` 与 `阶段结论` 为独立文件

### 3.2 命名规则

- 方案目录统一使用 `方案xx-名称`
- 轮次目录统一使用 `第01轮`、`第02轮`
- 输出图命名必须能够回溯到方案、轮次、模型与序号
- 图片复制或移动进仓库后，应按经过的流程方案重命名

### 3.3 文档同步规则

只要目录名、提示词名、方案名发生变化，必须同步更新以下四个真源：

- `docs/信息索引.md`
- `docs/文件结构定义.md`
- `docs/项目目录树.md`
- `.agents/PROGRESS.md`

### 3.4 阶段0归属规则

- 角色拆解、角色约束、角色标准化强相关的提示词，优先归到 `prompts/00-阶段0-原始元素处理/元素提取/`
- 草稿优化、抠图、背景处理、图像精修强相关的提示词，优先归到 `prompts/00-阶段0-原始元素处理/图像处理/`
- “只保留人体结构”的结果统一叫 `骨架图`
- 对应语义统一为：`角色设计正面图 -> 转骨架图`

---

## 4. 半抽象模板的落法

本次迁移不建议把当前仓彻底抽象成无行业特征的空骨架，也不建议一比一复制成只能服务当前项目的专用仓。

建议采用“半抽象模板”：

### 4.1 目录层

- 保留当前阶段化结构
- 不强制重命名目录
- 保留 `00-公共资源 / prompts / 01 / 02 / 03 / .agents / docs` 这一主骨架

### 4.2 文档层

- 将文档表达改写为“阶段化测试项目通用模板”
- `线稿 / 上色 / 动作` 可保留为默认示例阶段名
- 但应在文档中注明：新项目可按自己的目标图类型替换阶段含义

### 4.3 治理层

- `.agents/` 只保留稳定治理逻辑
- 不带当前项目的具体历史判断
- `PROGRESS.md` 只保留仍然重要的滑动索引
- 历史细节放在 `.agents/progress/entries/`

### 4.4 迁移边界

迁移时要复制“方法”，不要复制“当前项目案例”。

也就是说：

- 复制目录结构、规则、说明模板
- 不复制当前测试轮的具体结论
- 不复制当前角色的具体样例
- 不复制只对当前测试阶段成立的临时判断

---

## 5. 从操作记录提炼设计决策

不迁移 `prompts/操作记录.md` 原文。

但应把其中仍有长期价值的“为什么这样设计”提炼成模板级决策，写进新项目的说明文档或治理说明中。

### 5.1 提炼规则

- 只收敛稳定决策，不收录临时操作步骤
- 只写仓库中已有依据的原因，不补写主观推断
- 优先提炼以下类型：
  - 结构设计原因
  - 命名原因
  - 记录机制原因
  - 规则升级原因

### 5.2 示范锚点

以下三条已经可以直接当作稳定决策示范：

#### 锚点1：单文件测试记录优于拆文件

- 决策：每轮只保留一个 `第xx轮-测试记录.md`
- 原因：能减少“测试记录 / 问题总结 / 阶段结论”之间的重复表述，降低维护成本，回看时也更顺
- 依据来源：
  - `.agents/RULES.md`
  - `docs/文件结构定义.md`
  - `.agents/progress/entries/2026-04-14-测试记录改为单文件模式.md`

#### 锚点2：目录变更必须同步四个真源

- 决策：目录名、提示词名、方案名变化后，必须同步更新索引、结构定义、目录树、进度索引
- 原因：当前仓已经证明，真实目录一旦变动，如果不同步这四处，后续接手会迅速失真
- 依据来源：
  - `docs/信息索引.md`
  - `docs/文件结构定义.md`
  - `docs/项目目录树.md`
  - `.agents/PROGRESS.md`
  - `.agents/progress/entries/2026-04-14-手动调整后的索引与记录同步.md`

#### 锚点3：图像处理类提示词应集中到阶段0同一目录

- 决策：草稿优化、抠图、背景处理、图像精修统一归到 `prompts/00-阶段0-原始元素处理/图像处理/`
- 原因：这类节点本质上是测试前的公共预处理，集中管理后比散落在业务阶段目录里更稳定
- 依据来源：
  - `docs/文件结构定义.md`
  - `.agents/RULES.md`
  - `.agents/progress/entries/2026-04-20-草稿优化提示词有姿势版落地.md`

### 5.3 使用方式

后续如需继续提炼新的模板决策，建议沿用同一格式：

1. 决策是什么
2. 为什么这样做
3. 依据来自哪些真源

如果没有明确文档依据，就不要把它写成模板级结论。

---

## 6. 给 Taotaotao AI 的读取指令

把这份文件交给 `Taotaotao` 的 AI 时，应明确以下要求：

### 6.1 它的角色

它应该把本文件视为“迁移蓝图”，而不是“待自动执行补丁”。

它的任务应是帮助输出：

- 新项目建议目录骨架
- 需要复制的文档清单
- 需要保留/删除的内容清单
- 如要反馈回 `Taotaotao`，只输出可追加到既有文档体系的建议，不新增一组分散入口文件

### 6.2 访问方式说明

因为本文件放在**当前仓根目录**，`Taotaotao` 的 AI 默认不一定能直接访问。

使用时二选一：

1. 把这份文件内容手动粘贴给 `Taotaotao` 的 AI
2. 先手动复制这份文件到 `Taotaotao` 仓根目录，再让它读取

### 6.3 给 Taotaotao AI 的明确输入要求

在把文件交给它时，建议明确要求它：

- 不要把当前仓的具体测试样例当作模板一部分
- 不要输出多份分散的回灌入口文档
- 优先输出一份“新项目迁移清单”或“迁移执行建议”
- 若需要反馈给 `Taotaotao` 原项目，只给出可并入现有文档体系的建议

---

## Source Rules

本文件中的结论，只允许从以下真源提炼：

### 当前仓结构与规则

- `docs/文件结构定义.md`
- `docs/信息索引.md`
- `docs/项目目录树.md`

### 治理结论

- `.agents/RULES.md`
- `.agents/PROGRESS.md`
- `.agents/progress/entries/`

### 设计原因来源

- `prompts/操作记录.md`

### 提炼约束

- 结构事实优先取自 `docs/` 与 `.agents/`
- “为什么这样做”的原因优先取自 `操作记录` 与 `progress entries`
- 没有文档依据的原因，不写成模板结论
- 不搬运历史路径变更全过程，只保留最终稳定结论

---

## Manual Migration Flow

手动迁移时，建议按以下顺序操作：

1. 先阅读本文件 `项目模板迁移说明.md`
2. 按本文件说明复制目录骨架与治理骨架到新项目
3. 再复制模板文档，不复制具体测试产物
4. 在新项目中优先补齐以下关键文件：
   - `README.md`
   - `docs/信息索引.md`
   - `docs/文件结构定义.md`
   - `docs/项目目录树.md`
   - `.agents/RULES.md`
   - `.agents/PROGRESS.md`
5. 使用 `Taotaotao` 的 AI 时：
   - 直接把本文件内容粘贴给它，或
   - 先把本文件复制到 `Taotaotao` 仓根目录再让它读
6. 让它基于本文件做：
   - 新项目缺项检查
   - 目录与文档一致性检查
   - 模板规则遗漏检查

---

## Acceptance

本文件完成后，应满足以下验收标准：

- 不看当前对话，`Taotaotao` 的 AI 只读这一份文件，也能理解迁移目标和边界
- 它能区分“要迁移的模板能力”和“不要迁移的项目样例”
- 它能知道哪些规则必须保留，哪些只是历史痕迹
- 它不会被引导去给 `Taotaotao` 新建一堆分散入口文档
- 它能基于文中的示范锚点继续提炼其他稳定决策，而不是重新发明判断标准

---

## Assumptions

- 本文件放在**当前仓根目录**，不是 `Taotaotao` 根目录
- 迁移由人工手动执行，不需要自动迁移脚本
- `Taotaotao` 这次只作为“协助理解与校验模板”的 AI 读取端，不直接改造其仓结构
- `prompts/操作记录.md` 只作为“提炼设计原因”的来源，不作为原文迁移对象
