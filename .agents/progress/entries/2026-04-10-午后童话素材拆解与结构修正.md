# 2026-04-10 午后童话素材拆解与结构修正

## 基本信息

- 日期：2026-04-10
- 接手人 / AI：Codex
- 处理内容：对 `00-公共资源/原始素材/午后童话.pptx` 做第一轮自动拆解，并根据用户多次修正意见调整目录、文档和治理规则

## 原始动作

### 1. 素材定位与读取

- 在 `00-公共资源/原始素材/` 中定位到 `午后童话.pptx`
- 识别到该文件为可解包的 PPTX
- 读取到共 7 页幻灯片
- 抽取到页内文字内容与 `ppt/media/` 中的内嵌图片资源

### 2. 第一轮自动拆解

- 自动生成了 `00-公共资源/原始素材/午后童话-素材拆解.md`
- 自动生成了世界观文档
- 自动生成了 7 个角色目录下的人物介绍文档
- 自动提取并归档了角色参考图
- 自动生成过一批角色基础提示词草稿

## 用户修正意见与对应调整

### 修正 1：人物信息结构不对

用户要求：

- `人物信息` 的下一层直接是具体人物
- 每个具体人物的下一层应包含 `参考图`、`角色介绍文档`、以及补充说明

实际调整：

- 移除了 `人物信息` 下原来的公共占位层
- 保留并统一了 `角色01-艾琳娜` 到 `角色07-假面` 这类直接角色目录
- 将每个角色目录整理为以 `参考图/` 和 `角色介绍文档.md` 为主的结构
- 曾经短暂加入过 `补充说明/`，后续因内容越界被清空

### 修正 2：提示词草稿不该自动生成

用户要求：

- 角色基础提示词草稿不属于 AI 自动生成范围
- 这部分不应由 AI 代写

实际调整：

- 删除了自动生成的角色基础提示词草稿文件
- 保留了 `prompts/00-阶段0-原始元素处理/基础描述整理/` 目录结构
- 明确写入规则：除非用户明确要求，否则不自动替用户写提示词正文

### 修正 3：角色介绍文档必须保持原文

用户要求：

- `角色介绍文档` 不是总结，不是理解版，而是原文

实际调整：

- 删除了最初自动生成的“总结式角色介绍”
- 重新生成所有角色文档为“来源 + 页码 + 原文”格式
- 保证正文部分直接来自 PPT 中抽取的原始文本，不做概括性改写

### 修正 4：世界观文档也必须保持原文

用户要求：

- 不只角色介绍，世界观正文也要保持原文

实际调整：

- 删除了最初自动生成的概括版世界观说明
- 重新生成 `00-公共资源/世界观设定/午后童话/世界观说明.md`
- 格式改为“来源 + 页码 + 原文”

### 修正 5：治理体系只做轻量引入

用户要求：

- 从 `C:\code\VScode\Taotaotao` 引入治理体系思路
- 只需要多 AI 连续对话记录等轻量能力
- 根目录不要太乱

实际调整：

- 根目录只新增 `AGENTS.md`
- 其余治理文件集中放入 `.agents/`
- 新增：
  - `.agents/RULES.md`
  - `.agents/PROGRESS.md`
  - `.agents/ITERATION_PLAN.md`
  - `.agents/progress/ENTRY_TEMPLATE.md`
  - 对应的中文说明文件

## 当前最终落地状态

### 素材落地

- 原始素材保留在 `00-公共资源/原始素材/午后童话.pptx`
- 拆解过程说明保留在 `00-公共资源/原始素材/午后童话-素材拆解.md`
- 世界观原文保留在 `00-公共资源/世界观设定/午后童话/世界观说明.md`
- 角色原文分别保留在各角色目录下的 `角色介绍文档.md`
- 参考图已按角色归档到各角色目录下的 `参考图/`

### 规则落地

- 原始素材正文类内容保持原文
- 不自动生成提示词正文
- 人物信息下一层直接是具体角色
- 图片移动或复制后需要按流程方案重命名
- 多 AI 连续对话记录进入 `.agents/PROGRESS.md`

## 这次过程中暴露出的能力边界

- 可以稳定读取 PPTX 的文字层
- 可以提取 PPTX 内嵌图片并按页级归档
- 可以按用户规则调整目录与文档结构
- 不能把“总结版”误当成“原文版”
- 不能在未获明确授权时替用户生成提示词正文
- 对同页多张参考图的更细粒度视觉分类，仍需要用户人工判断或额外授权

## 交接备注

- 下一位接手者如果继续处理 `午后童话`，应先看本记录
- 其次查看 `.agents/RULES.md`
- 再查看具体角色目录与 `00-公共资源/原始素材/午后童话-素材拆解.md`
